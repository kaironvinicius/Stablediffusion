[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint.per-file-ignores]
# The smoke test runs as a standalone script, so it puts the repository root on
# sys.path before importing from it. That import order is the point, not a slip.
"tests/*" = ["E402"]
